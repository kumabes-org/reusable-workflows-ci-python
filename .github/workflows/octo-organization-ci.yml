name: Octo Organization CI

on: push

jobs:
  build:
    runs-on: kumabes-runner

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello from Octo Organization